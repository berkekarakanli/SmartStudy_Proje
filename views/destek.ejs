<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SmartStudy | Destek</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&family=Inter:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        body { background:#020617; color:#f8fafc; font-family:'Inter',sans-serif; min-height:100vh; padding: 40px 16px; }
        .orbitron { font-family:'Orbitron', sans-serif; }
        .panel { max-width:600px; margin:0 auto; background:rgba(30,41,59,.7); border:1px solid rgba(13,202,240,.25); border-radius:20px; padding:36px 28px; box-sizing:border-box; }
        .form-control { background:#0f172a; border:1px solid #334155; color:white!important; padding:12px; }
        .form-control::placeholder { color:#64748b; }
        .btn-send { background: linear-gradient(45deg, #0dcaf0, #007bff); border:none; color:#000; font-weight:800; padding:14px; border-radius:12px; width:100%; }
    </style>
</head>
<body>
    <div class="panel">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="h4 text-info orbitron mb-0"><i class="fas fa-headset me-2"></i>Destek</h1>
            <a href="/" class="btn btn-sm btn-outline-info">Ana Sayfa</a>
        </div>

        <% if (sent) { %>
            <div class="alert alert-success">
                <i class="fas fa-check-circle me-2"></i>Talebin bize ulaştı! En kısa sürede e-posta ile geri döneceğiz.
            </div>
            <a href="/" class="btn btn-outline-info w-100">Ana Sayfaya Dön</a>
        <% } else { %>
            <p class="text-secondary small mb-4">Soru, öneri ya da şikayetini aşağıdan iletebilirsin — sana en kısa sürede e-posta ile dönüş yapılır.</p>

            <% if (typeof error !== 'undefined' && error) { %>
                <div class="alert alert-danger small"><%= error %></div>
            <% } %>

            <form action="/destek" method="POST" class="row g-3">
                <div class="col-md-6">
                    <label class="form-label small">Ad Soyad</label>
                    <input class="form-control" name="ad" value="<%= user ? user.ad : '' %>" required>
                </div>
                <div class="col-md-6">
                    <label class="form-label small">E-Posta</label>
                    <input class="form-control" type="email" name="email" value="<%= user ? user.email : '' %>" required>
                </div>
                <div class="col-12">
                    <label class="form-label small">Konu</label>
                    <select class="form-control" name="konu">
                        <option value="Genel Soru">Genel Soru</option>
                        <option value="Teknik Sorun">Teknik Sorun</option>
                        <option value="Premium / Ödeme">Premium / Ödeme</option>
                        <option value="Şikayet">Şikayet</option>
                        <option value="Diğer">Diğer</option>
                    </select>
                </div>
                <div class="col-12">
                    <label class="form-label small">Mesajın</label>
                    <textarea class="form-control" name="mesaj" rows="5" placeholder="Bize ne yazmak istersin?" required></textarea>
                </div>
                <div class="col-12 mt-4">
                    <button class="btn-send orbitron">Gönder</button>
                </div>
            </form>
        <% } %>
    </div>
</body>
</html>
